{
  "name": "docker-lambda-benchmark",
  "version": "1.0.0",
  "description": "Benchmark docker lambda cold start vs zip lambda cold start",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.121",
    "@types/node": "^20.6.2",
    "aws-lambda": "^1.0.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
